если захочется вылизать проект (вряд ли):

- кнопка ресета должна просить подтверждение
- управление конфигом:
    - управление сетями
        - просить имя и рпс перед созданием
        - изменить рпс или имя
    - управление токенами для сетей
        - изменить символы и адрес
        - добавлять токен с дефолтными данными (функционал есть, не привязано к кнопке)
- управление профилями:
    - смена имени
    - все через кнопки и стейты